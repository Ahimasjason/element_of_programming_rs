mod convert;
mod convert_base;
mod palindrome;
mod spread_sheet;
mod replace_and_remove;
mod palindromicity;
mod reverse_word;
mod phone_mnemonic;
mod look_and_say;
mod roman;
mod valid_ip_addresses;
mod sinusoidal;

mod rle;


#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
